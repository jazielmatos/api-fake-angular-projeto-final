# Instruções

```shell
# rodar códigos abaixo
npm install
npm run dev # para desenvolvimento
npm start # para produção
# Fazendo post via terminal
 curl -d '{"id":"152", "nome":"Sergio"}' -H "Content-Type: application/json" -X POST http://localhost:3000/clientes
```